apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: k8sexplorer
resources:
  - namespace.yaml
  - rbac.yaml
  - deployment.yaml
images:
  - name: your-registry.example.com/k8sexplorer
    newTag: latest # CI (or Flux/Argo CD image automation) rewrites this after each Kaniko build
