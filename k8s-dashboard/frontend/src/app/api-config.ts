// Relative on purpose — see product-catalog app's api-config.ts for the
// full reasoning (works unchanged via ng serve's proxy, a Docker container,
// or a future Kubernetes deployment).
export const API_BASE_URL = '/api';
